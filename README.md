# Story Summarizer

Install Pipenv

    ```pip install pipenv```

Install Requirements

    ```pipenv install```

Activate virtual environment

    ```pipenv shell```

Run App

    ```python app.py```